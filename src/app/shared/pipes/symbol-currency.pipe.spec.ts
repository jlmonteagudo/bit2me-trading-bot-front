import { SymbolCurrencyPipe } from './symbol-currency.pipe';

describe('SymbolCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
